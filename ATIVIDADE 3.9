#include <stdio.h>

int main()
{
  int x, i;
  int fatorial;
  fatorial = 1;
  
  printf("Digite o número inteiro positivo: ");
  scanf("%i", &x);

  for(i = 1; i <= x; i++)
  fatorial = fatorial * i;

  printf("O fatorial de %d é: %d\n", x, fatorial);
  
  return 0;
  

}
